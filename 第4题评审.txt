题目4：
a. geoHelperAddress()
1. 缓存key直接使用地址拼接不是很合适，可以考虑md5(地址)再拼接组成缓存key。
2. 调用接口失败的判断可以使用一次判断就可以了，可以考虑合并接口失败的判断，包括: $data['error'], $coordinate内容的判断
3. 如需要使用商家ID返回坐标信息，为何此处没有写入缓存呢？另外，缓存是否可以加上时效？
4. 函数返回字符串形式的坐标信息是否不方便调用方使用处理，是否可以使用数组形式
5. 函数是否可以减少使用if方式做选择使用地址或者商家ID来做获取坐标，是否可以分解成独立的两个函数，一个专门通过地址传参返回坐标，另一个则通过商家ID传参返回坐标
6. 返回内容描述，是否需要说明坐标结果的xy是那个部分？对于使用方更清楚结果

b. checkStatusCallback()
1. 不是怎么明白这函数用途的目的和意义？函数返回结果值，一时是数字，一时是字符串，令人困惑。
2. 如用途所说是做状态码的转换，为什么还要转递订单号？是否不同的订单输入同一状态码都是有不同意思？
